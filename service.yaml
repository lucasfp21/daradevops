apiVersion: v1
kind: Service
metadata:
  name: hello-python
  namespace: links
spec:
  type: NodePort
  selector:
    app: hello-python
  ports:
    - port: 9999
      targetPort: 9999
      nodePort: 30999